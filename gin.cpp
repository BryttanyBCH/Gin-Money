#include"card.h"
//I think we don't need to make a class for the decks as they are just going to be vectors of cards, just like the player's hand
//Since the cards are going to be moving all over the place, they should be pointers

#include<iostream> //for output
#include<algorithm> //for shuffle
#include<vector> //for decks

using namespace std; //idealy we would use std::

int main() {
  return 0;
}
